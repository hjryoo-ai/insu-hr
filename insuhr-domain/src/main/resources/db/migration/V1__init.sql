-- V1__init.sql — 스키마 베이스라인 (설계서 13.2 Phase 0)
--
-- 업무 테이블을 만들지 않는다. Flyway 연결·마이그레이션 파이프라인이 동작하는지 확인하는
-- 베이스라인이며, 실제 스키마는 V2 이후에 쌓는다.
--
-- 규칙(설계서 13.1): 스키마 변경은 오직 이 디렉터리의 마이그레이션 파일로만 한다.
-- 엔티티 ddl-auto 로 스키마를 만들지 않는다(validate 만 허용).

-- Flyway는 실행 가능한 구문이 없는 마이그레이션도 정상 적용으로 기록한다.
-- 여기서 아무것도 만들지 않는 것이 의도다.
BEGIN
    NULL;
END;
/
